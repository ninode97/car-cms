import React from "react";

type GridTableActionsProps = {
  children?: JSX.Element[] | JSX.Element;
};

const GridTableActions: React.FC<GridTableActionsProps> = ({ children }) => {
  return <div>GridTableActions</div>;
};

export default GridTableActions;
